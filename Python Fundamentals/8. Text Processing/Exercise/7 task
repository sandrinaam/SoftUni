text = list(map(str, input()))
strength = 0
result = ''

for i in range(len(text)):
    if text[i] == '>':
        result += '>'
        strength += int(text[i+1])
    elif strength > 0:
        strength -= 1
    else:
        result += text[i]

print(result)
