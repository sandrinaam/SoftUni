def factorial_division(number):
    factorial = number
    for digit in range(1, number):
        factorial *= digit
    return factorial

first_number = int(input())
second_number = int(input())
first_factor = factorial_division(first_number)
second_factor = factorial_division(second_number)
result = first_factor / second_factor
print(f'{result:.2f}')

