import re
string = input()
pattern = r'(@|#)+([a-z]{3,})(@|#)+([^A-Za-z0-9])*\/{1,}(\d+)'
matches = re.findall(pattern, string)

for match in matches:
    color = match[1]
    count = match[4]
    print(f'You found {count} {color} eggs!')
