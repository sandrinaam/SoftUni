key = int(input())
number_of_lines = int(input())
message = ''
for i in range(number_of_lines):
    letter = input()
    ord_letter = ord(letter)
    ord_new_letter = ord_letter + key
    message += chr(ord_new_letter)
print(message)
