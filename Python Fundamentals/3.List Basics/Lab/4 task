number = int(input())
word = input()
strings_list = []
filtered_list = []

for _ in range(number):
    _string = input()
    strings_list.append(_string)
    if word in _string:
        filtered_list.append(_string)

print(strings_list)
print(filtered_list)

