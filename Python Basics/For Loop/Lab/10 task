n = int(input())
sum_even = 0
sum_odd = 0

for i in range(n):
    new_number = int(input())
    if i % 2 == 0:
        sum_even += new_number
    else:
        sum_odd += new_number

if sum_odd == sum_even:
    print(f"Yes\nSum = {sum_odd}")
else:
    print(f"No\nDiff = {abs(sum_odd-sum_even)}")
