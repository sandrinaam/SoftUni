number_pages = int(input())
number_pages_per_hour = int(input())
number_days = int(input())

hours_needed = (number_pages / number_pages_per_hour)
hours_per_day = hours_needed / number_days
print(int(hours_per_day))





